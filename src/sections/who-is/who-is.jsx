import styles from './who-is.module.css';
function WhoIs() {

    return (
        <section className={styles["who-is"] + ' flex justify-center content-center'}>
            <p> This is a test</p>

        </section>
    )
}

export default WhoIs
